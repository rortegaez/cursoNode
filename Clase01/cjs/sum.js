function sum (a, b) {
  return a + b
}

// con este formato le obligamos a que en el archivo al cual se exporte, se tenga que escribir igual
// module.exports = sum con este formato en el archivo exportados e puede escribir como quieras
module.exports = {
  sum
}
